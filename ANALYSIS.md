# 愛を伝えるだとか — 音源解析メモ

解析対象は `/Users/konaito/Downloads/愛を伝えるだとか.wav`。数値はローカルで `ffprobe` と NumPy のスペクトル解析を実行した結果です。

## ファイル構造

| 項目 | 実測値 |
| --- | --- |
| コンテナ | WAV |
| コーデック | PCM signed 16-bit little-endian |
| サンプルレート | 48,000 Hz |
| チャンネル | 2（ステレオ） |
| フレーム数 | 11,114,880 |
| 再生時間 | 231.560 秒 |
| ビットレート | 1,536 kbps |
| タグ | made with suno / id `f8f9e76b-a0b1-40b1-afca-d39f0b1d226f` |

## 音楽的な推定

- テンポ: **133 BPM**（スペクトルフラックスの自己相関で最大ピーク）
- 調: **D major 推定**（クロマベクトルとKrumhansl型テンプレートの相関）
- 歌詞グリッド: 1小節を `4 × 60 / 133 = 1.8045秒` として、8小節イントロから128小節分を配置
- 構成: Intro → Verse 1 → Pre-chorus → Chorus → Instrumental → Verse 2 → Pre-chorus → Chorus → Bridge → Final chorus

調とテンポは自動推定値であり、楽譜やDAWのプロジェクトデータから取得した確定値ではありません。カラオケ表示は歌唱しやすさを優先して、このグリッドを基準にしています。

## ボーカル低減の判断

元WAVをモノラル化したRMSは約`0.1041`、左右差（side）のRMSは約`0.0307`、左右チャンネル相関は`0.8407`でした。中央定位が強いので、左右の差だけを残す単純なセンターキャンセルが候補になります。

ただし単純なセンターキャンセルはキック、ベース、スネアなども削ります。そこでアプリのWASM DSPは、

1. 左右のmid（中央）とside（左右差）を作る
2. midのうち160 Hz以下を1ポールLPFで保持する
3. 160 Hz以上の中央成分だけを`amount`に応じて抑える
4. sideと再合成する

という方式にしています。`ボーカル低減`は72%、`ボーカル強め`は94%です。これは完成ミックスからの**ボーカル低減**であり、分離ステムが存在しない以上、すべての声を完全に消す処理ではありません。この限界を画面とREADMEにも明記しています。

## 歌詞の出典と配置

歌詞は同じ曲のM4Aに埋め込まれていた`lyrics`タグから抽出しました。アプリの`karaoke/song-data.js`には、歌詞本文と、上記133 BPMの小節グリッドから算出した表示時刻を保存しています。

## アプリ側での解析

- WASM: ボーカル低減、RMS、マイク自己相関ピッチ検出
- Web Audio API: 音源デコード、再生、波形表示、マイク入力
- Service Worker: アプリ本体・WASM・音源をキャッシュ
- ネットワーク: 初回ロード後は音源も解析も外部サーバーへ送信しない
