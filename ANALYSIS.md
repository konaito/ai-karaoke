# 愛を伝えるだとか — 音源解析メモ

解析対象は `/Users/konaito/Downloads/愛を伝えるだとか.wav`。数値はローカルで `ffprobe` と NumPy のスペクトル解析を実行した結果です。

## ファイル構造

| 項目 | 実測値 |
| --- | --- |
| コンテナ | WAV |
| コーデック | PCM signed 16-bit little-endian |
| サンプルレート | 48,000 Hz |
| チャンネル | 2（ステレオ） |
| フレーム数 | 11,114,880 |
| 再生時間 | 231.560 秒 |
| ビットレート | 1,536 kbps |
| タグ | made with suno / id `f8f9e76b-a0b1-40b1-afca-d39f0b1d226f` |

## 音楽的な推定

- テンポ: **133 BPM**（スペクトルフラックスの自己相関で最大ピーク）
- 調: **D major 推定**（クロマベクトルとKrumhansl型テンプレートの相関）
- 歌詞グリッド: 1小節を `4 × 60 / 133 = 1.8045秒` として、8小節イントロから128小節分を配置
- 構成: Intro → Verse 1 → Pre-chorus → Chorus → Instrumental → Verse 2 → Pre-chorus → Chorus → Bridge → Final chorus

調とテンポは自動推定値であり、楽譜やDAWのプロジェクトデータから取得した確定値ではありません。カラオケ表示は歌唱しやすさを優先して、このグリッドを基準にしています。

## ボーカル低減の判断

元WAVをモノラル化したRMSは約`0.1041`、左右差（side）のRMSは約`0.0307`、左右チャンネル相関は`0.8407`でした。中央定位が強いので、左右の差だけを残す単純なセンターキャンセルが候補になります。

ただし単純なセンターキャンセルはキック、ベース、スネアなども削ります。そこでアプリのWASM DSPは、

1. 左右のmid（中央）とside（左右差）を作る
2. midのうち160 Hz以下を1ポールLPFで保持する
3. 160 Hz以上の中央成分だけを`amount`に応じて抑える
4. sideと再合成する

という方式にしています。`ボーカル低減`は72%、`ボーカル強め`は94%です。これは完成ミックスからの**ボーカル低減**であり、分離ステムが存在しない以上、すべての声を完全に消す処理ではありません。この限界を画面とREADMEにも明記しています。

## 歌詞の出典と配置

歌詞は同じ曲のM4Aに埋め込まれていた`lyrics`タグから抽出しました。歌手名は`konaito`、曲名は`愛を伝えるだとか`です。`CHORUS`や`VERSE`は歌唱セクション名であり、歌手名ではありません。

旧版の2小節固定配置は廃止し、`karaoke/alignment.json`にローカル音声認識から得た56行分の文字タイムラインを保存しています。既知の正しい歌詞をプロンプトとしてセクションごとに音声認識し、返された日本語の単語タイムスタンプを文字へ展開しました。行内の各文字は、行全体の長さを均等配分せず、個別の`start`/`end`を使って色が進みます。

今回の方式は**既知歌詞付きASR文字アライメント**です。従来の行単位グリッドより厳密ですが、歌唱専用の音素強制アライメント（「き」「み」などの音素境界を音響モデルで直接推定）ではありません。`confidence`も保存し、認識が曖昧な箇所を無言で高精度に見せないようにしています。自動処理だけで完全一致を保証するのではなく、最終的には低信頼度区間を実際に聴いて補正するレビュー工程が必要です。

主な実測開始時刻は、1番Aメロが約`13.080`秒、1回目のサビが約`55.600`秒、2番Aメロが約`93.840`秒、2回目のサビが約`136.560`秒、最後のサビが約`191.600`秒です。旧BPMグリッドでは2番以降が数秒ずつ後ろへずれていたため、今回の置き換えで曲全体の累積ズレを解消しています。

## アプリ側での解析

- WASM: ボーカル低減、RMS、マイク自己相関ピッチ検出
- Web Audio API: 音源デコード、再生、音源から計算した波形表示、マイク入力
- Service Worker: アプリ本体・WASM・音源をキャッシュ
- ネットワーク: 初回ロード後は音源も解析も外部サーバーへ送信しない

## 日本のカラオケ採点システムの調査

2026-09-11にDAMとJOYSOUNDの公式説明を確認した。両社の採点は、歌詞を色で追うだけの仕組みではなく、正解として登録されたガイドメロディーとマイク入力を時間・音高方向に比較する歌唱中の表示と、歌唱後に全体を集計する結果表示に分かれている。

| 領域 | DAM 精密採点Ai / DX-G | JOYSOUND 分析採点AI | このアプリの現在地 |
| --- | --- | --- | --- |
| 歌唱中 | 見えるガイドメロディー、歌唱軌跡、技法カウンター、Ai感性メーター | 区間ごとの音程一致率、技法カウント、AIゲージ | 歌詞の文字タイムライン、任意のマイク音程モニター |
| 基本評価 | 音程、安定性、表現力、ビブラート等。DX-Gでは複数の計算軸も使用 | 音程、安定感、抑揚、ロングトーン、テクニック | 未実装。採点済みとは表示しない |
| 独自評価 | Ai感性、Ai感性ボーナス、歌唱技法の演出 | AIゲージ、AIボーナス、歌唱技法の演出 | 未実装 |
| 歌唱後 | 総合点、レーダー／区間グラフ、分析レポート | 総合点、5項目、AIボーナス、総評、音域等 | 未実装 |

DAM公式は、精密採点Aiの歌唱中にガイドメロディーを上部に表示し、音程が概ね正しい場合は黄色、より正確な場合は金色に歌唱軌跡を変えると説明している。また、しゃくり・こぶし・フォール・ビブラートは検出して技法カウンターへ送る。JOYSOUND公式は、区間ごとの音程一致率、技法アイコン／回数、AIゲージを歌唱中に表示し、結果画面で音程・安定感・抑揚・ロングトーン・テクニックとAIボーナスを確認すると説明している。

参照した公式ページ: [DAM 精密採点Ai](https://www.clubdam.com/contents/saiten/seimitsusaiten_ai.html)、[DAM 精密採点DX-G](https://www.clubdam.com/contents/saiten/seimitsusaiten_dx_g.html)、[DAM 採点開発者インタビュー](https://www.clubdam.com/recommend/enjoy/saiten_interview07.html)、[JOYSOUND 分析採点AI＋/AI](https://www.joysound.com/web/s/joy/bunseki)、[JOYSOUND 分析採点の遊び方](https://www.joysound.com/web/s/function/bunseki)。

## 採点を実装するために必要な解析

今の`alignment.json`は歌詞表示のための文字タイムラインであり、正解メロディーの音符列ではない。日本式の採点を「それらしく」ではなく検証可能に実装するには、次のデータを別途生成する必要がある。

1. 原曲から主旋律の音符列（開始・終了・MIDI音高・休符）を作る。完成ミックスからの自動抽出は伴奏・ハモリ・残響の影響を受けるため、低信頼区間を耳で補正する。
2. マイクは`AudioWorklet`またはWASMで短いフレームに分け、声の有無、基本周波数、RMS、発声開始／終了を記録する。
3. 正解音符と歌唱フレームを時刻で比較し、音程一致、入りの遅速、ロングトーンの維持、音程の揺れ、声量の区間差を集計する。
4. しゃくり・こぶし・ビブラートは音程軌跡から個別に検出する。ただしDAM／JOYSOUND固有の閾値と重みは公開仕様ではないため、同社と同じ点数になるとは保証できない。
5. 歌唱中はガイドメロディー＋歌唱軌跡＋区間ゲージだけを表示し、歌唱後に総合点・項目別点・区間グラフ・改善コメントを別画面で表示する。

現時点では音源なしのDOM／画面テストを行い、実際の再生やマイク採点は実行していない。これはUIの確認であり、音程採点の精度検証ではない。

## UIの表示方針

メイン画面は`100dvh`に拘束した**スクロールレスUI / 一画面完結型**です。日本の一般的な歌唱画面に寄せ、背景映像を主役にし、現在行を大きな中央寄りの2行テロップ、次の行を下の予告として表示します。音程モニターは通常歌唱画面に常時押し込まず、必要時だけオーバーレイで開きます。曲構成・キー・ボーカル低減・音量は設定ドロワーへ移動しました。

これは歌詞タイミングの精度を上げる解析手法ではありません。現在の歌詞タイムラインが行単位である限り、表示をスクロールレスにしても、音素・モーラ単位のアライメントが未実装という精度上の制約は残ります。
