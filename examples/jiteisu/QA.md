# 今回の生成と確認

2026-09-11。対象はユーザー指定の YouTube `XgYgRNQJ13A`、タイトル「時定数の低い世界で」、歌手 konaito。原文は `lyrics-original.txt` に保存。

- 曲末の括弧内の一行は、音源の認識結果に合わせて「20代はおしまい」「40代ははじまり」を2回ずつの4行へ展開。全45行。
- 音源・認識条件・固定モデル・依存バージョンは `source.json` と `transcripts.provenance.json`。認識結果は `transcripts.json`。
- 元曲の回収コードによる既存JSON全体の完全一致、新曲の再生成、読みと表示の対応、破損入力の拒否など、6テスト成功。
- 新曲を2回ビルドし、HTML・JS・CSS・WASM・音源・歌詞・キャッシュ定義など全出力ファイルのバイト一致を確認。
- Codexブラウザで新曲の曲名、45行、3分32秒、再生開始、経過時間の進行、文字の色進行、165秒のサビ、199.5秒の終盤へのシークを確認。
- 以前使ったlocalhostポートでは旧Service Workerの画面が出たため、新しいオリジン `http://127.0.0.1:4287/` で確認。既存ポートを再利用するときは更新を適用する。

`alignment.review.json` に補間した17文字を残している。主に冒頭の2025年、括弧内のロジック、2回目サビの一部、ああ、終盤の40。行の重複・トークン境界の警告は0件。これは試聴によるタイミング正解の保証ではない。全曲の耳による同期評価・実機モバイル確認・公開デプロイは今回の確認に含めていない。
